#!/usr/bin/env bash
# script/test: Run tests

set -e

cd "$(dirname "$0")/.."

echo "==> Running tests..."

if command -v uv >/dev/null 2>&1; then
  uv run --no-project pytest "$@"
else
  pytest "$@"
fi
