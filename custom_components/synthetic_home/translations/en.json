{
  "config": {
    "step": {
      "user": {
        "title": "Synthetic Home",
        "description": "Please enter a filename for a `.yaml` file in your `config/` directory that will be parsed as Synthetic Home data. If you need help with the configuration have a look here: https://github.com/allenporter/synthetic-home",
        "data": {
          "config_filename": "Config Filename"
        }
      }
    },
    "error": {
      "does_not_exist": "The configuration file does not exist in your `config/` directory."
    },
    "abort": {}
  }
}
